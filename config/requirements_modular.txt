# Redfish Mockup Server Dependencies

# Core HTTP and networking
requests>=2.20.0
grequests>=0.6.0
requests-toolbelt>=0.9.0

# Optional SSDP support (requires gevent)
gevent>=1.4.0

# Data classes for Python < 3.7 (if needed)
dataclasses>=0.6;python_version<"3.7"